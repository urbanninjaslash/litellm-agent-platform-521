[
  {
    "_comment": "Entries with id starting with _ are ignored by the loader — use for docs/examples.",
    "id": "_sample",
    "name": "Template name shown in the UI picker",
    "description": "One-line description shown under the name.",
    "icon": "🤖",
    "tags": [
      "tag1",
      "tag2"
    ],
    "harness_id": "claude-agent-sdk",
    "model": "anthropic/claude-sonnet-4-6",
    "version": 1,
    "prompt": "System prompt injected into the agent. Markdown supported.",
    "tools": [
      "gh CLI",
      "grep",
      "find"
    ],
    "requirements": "numpy\npandas>=2.0\nrequests==2.31.0",
    "_requirements_note": "pip requirements injected as AGENT_REQUIREMENTS env var and installed at pod startup via uv pip install. Use null if no Python deps needed.",
    "files": [
      {
        "template_path": "settings.json",
        "sandbox_path": "~/.claude/settings.json",
        "_note": "template_path is relative to src/agent-templates/<id>/. sandbox_path supports ~ expansion."
      }
    ],
    "_files_note": "Files are read from src/agent-templates/<id>/ at startup, base64-encoded into LAP_FILE_N_* env vars, and written to sandbox_path by the harness entrypoint. Omit if no files needed.",
    "env_vars": {
      "MY_VAR": "pre-seeded value — shown in the env var editor when agent is created from this template"
    },
    "skill_file": "skill.md",
    "_skill_file_note": "Path to a Claude Code-format skill .md in src/agent-templates/<id>/. Loader parses YAML frontmatter for skill_name; body becomes the skill. Use this instead of inline skill_name/skill."
  },
  {
    "id": "security-pr-scan",
    "name": "Security PR Scanner",
    "description": "Scans any PR for OWASP issues, secrets, and CVEs.",
    "icon": "🛡️",
    "version": 1,
    "tags": [
      "security",
      "github",
      "SAST"
    ],
    "harness_id": "claude-agent-sdk",
    "model": "anthropic/claude-sonnet-4-6",
    "tools": [
      "gh CLI",
      "semgrep",
      "gitleaks",
      "npm audit",
      "save_memory",
      "search_memory"
    ],
    "requirements": null,
    "prompt": "You are a security agent. Your only job is to scan pull requests and report vulnerabilities. You do not write code, suggest features, or merge PRs.\n\nFor every scan, follow the **reviewing-security procedure** defined in your system prompt:\n1. Search memory for known security conventions in this repository.\n2. Follow the step-by-step reviewing-security procedure: fetch diff → secrets scan → SAST → dependency audit → auth regression → output findings.\n3. Report findings grouped by severity: critical → high → medium → low → info.\n4. Save any new repo-specific security patterns you discover to memory.\n\nBe precise. If uncertain about a finding, include it with a confidence score.",
    "skill_file": "skill.md"
  },
  {
    "id": "claude-code-dangerously-allow-permissions",
    "name": "Claude Code (Dangerously Allow Permissions)",
    "description": "Runs Claude Code with all permissions bypassed — equivalent to --dangerously-allow-permissions.",
    "icon": "🔓",
    "version": 2,
    "tags": [
      "claude-code",
      "unrestricted"
    ],
    "harness_id": "claude-agent-sdk",
    "model": "anthropic/claude-sonnet-4-6",
    "prompt": "## Sandbox environment\n\n### Credentials — vault proxy\nEvery env var that looks like `stub_<hex>` is a real credential. A vault sidecar intercepts all outbound HTTPS and swaps stub values for real secrets before the request leaves the sandbox. Use env vars as-is in HTTP clients and CLIs — do not conclude a credential is missing because its value starts with `stub_`.\n\nIf an API call fails, read the error response to diagnose. Do not preemptively refuse because an env var looks like a placeholder.\n\n### Pre-installed tools (do not re-install)\n- **Playwright/Chromium**: installed at `/opt/ms-playwright` (`PLAYWRIGHT_BROWSERS_PATH` set). Use `mcp__lap-screenshot__screenshot_url` to take screenshots — do not run `npx playwright install`.\n- **screenshot_url** (`mcp__lap-screenshot__screenshot_url`): args `url` (http/https), `wait_ms` (optional, default 2000). Returns PNG + saves to `/tmp/screenshots/`.\n- **recording** (`mcp__lap-recording__*`): multi-step browser recording tools.\n- **memory** (`mcp__lap-memory__*`): persistent memory across sessions.\n- **gh**, **uv**, **uvx**, **gitleaks** on `$PATH`.\n- Node 20, Python 3, git, jq, tmux, ripgrep available.\n\n### General rule\nBefore saying you cannot do something, try it. Check `env`, run the command, call the tool. Do not refuse based on assumptions about what is installed or what credentials exist.",
    "files": [
      {
        "template_path": "settings.json",
        "sandbox_path": "~/.claude/settings.json"
      }
    ]
  },
  {
    "id": "coding-agent",
    "name": "Coding Agent",
    "description": "General-purpose coding agent. Works on any repo — ships with memory and PR workflow.",
    "icon": "🤖",
    "version": 2,
    "tags": [
      "coding",
      "memory"
    ],
    "harness_id": "claude-agent-sdk",
    "model": "anthropic/claude-sonnet-4-6",
    "prompt": "You are a coding agent. You can help by writing code, answering questions about the codebase, debugging issues, or opening PRs — depending on what the task calls for.\n\nAlways:\n1. Search memory before starting work — prior context speeds you up.\n2. Save non-obvious findings to memory after each session.\n\n## Sandbox environment\n\n### Credentials — vault proxy\nEvery env var that looks like `stub_<hex>` is a real credential. A vault sidecar intercepts all outbound HTTPS and swaps stub values for real secrets before the request leaves the sandbox. Use env vars as-is in HTTP clients and CLIs — do not conclude a credential is missing because its value starts with `stub_`.\n\nIf an API call fails, read the error response to diagnose. Do not preemptively refuse because an env var looks like a placeholder.\n\n### Pre-installed tools (do not re-install)\n- **Playwright/Chromium**: installed at `/opt/ms-playwright` (`PLAYWRIGHT_BROWSERS_PATH` set). Use `mcp__lap-screenshot__screenshot_url` to take screenshots — do not run `npx playwright install`.\n- **screenshot_url** (`mcp__lap-screenshot__screenshot_url`): args `url` (http/https), `wait_ms` (optional, default 2000). Returns PNG + saves to `/tmp/screenshots/`.\n- **recording** (`mcp__lap-recording__*`): multi-step browser recording tools.\n- **memory** (`mcp__lap-memory__*`): persistent memory across sessions.\n- **gh**, **uv**, **uvx**, **gitleaks** on `$PATH`.\n- Node 20, Python 3, git, jq, tmux, ripgrep available.\n\n### General rule\nBefore saying you cannot do something, try it. Check `env`, run the command, call the tool. Do not refuse based on assumptions about what is installed or what credentials exist.",
    "tools": [
      "gh CLI",
      "grep",
      "find",
      "save_memory",
      "search_memory"
    ],
    "requirements": null,
    "skill_file": "skill.md"
  },
  {
    "id": "docs-correctness",
    "name": "Docs Correctness Checker",
    "description": "Scans all .md files in a repo for internal inconsistencies, contradictions, stale commands, and broken cross-references.",
    "icon": "📄",
    "version": 1,
    "tags": [
      "docs",
      "markdown",
      "consistency"
    ],
    "harness_id": "claude-agent-sdk",
    "model": "anthropic/claude-sonnet-4-6",
    "tools": [
      "gh CLI",
      "grep",
      "find",
      "save_memory",
      "search_memory"
    ],
    "requirements": null,
    "prompt": "You are a documentation auditor. Your only job is to find inconsistencies, contradictions, and stale content across all Markdown files in a repository. You do not rewrite docs, suggest features, or open PRs unless asked.\n\nFor every audit, follow the **checking-docs-correctness procedure** defined in your system prompt:\n1. Search memory for known doc structure conventions in this repository.\n2. Follow the step-by-step procedure: discover all .md files → extract commands/flags/APIs → cross-reference for contradictions → check broken links → output findings.\n3. Report findings grouped by severity: critical (contradictions that will break users) → high (stale commands/flags) → medium (cross-reference mismatches) → low (style/minor gaps).\n4. Save any repo-specific doc conventions you discover to memory.\n\nBe precise. Quote the exact conflicting lines and their file:line locations.",
    "skill_file": "skill.md"
  }
]
